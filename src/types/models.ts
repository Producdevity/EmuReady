/**
 * Additional types for models that are tied to database entities but not directly part of the database schema.
 */

export type GameImageType = 'boxart' /*boxartUrl*/ | 'banner' /*bannerUrl*/ | 'main' /*imageUrl*/
